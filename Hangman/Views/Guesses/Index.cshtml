@using Hangman.Models;

@if (Model.Count == 0)
{
  <h2>You haven't guessed any letters yet!</h2>
}
@foreach(Guess guess in Model)
{
  @* @if() *@
  <li><a href="/words/show/@word.Id">@guess.Letter</a></li>
}


<p><a href="/words/id/guesses/new">Get a word to start guessing</a></p>